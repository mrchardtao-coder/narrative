/* ========================================
   常量配置
   ======================================== */
const CONFIG = {
  DEFAULT_ATTENTION: 5,
  MEMORY_CHAR_TO_TOKEN: 0.5,
  MEMORY_COMPRESS_THRESHOLD: 500000,
  MAX_HISTORY: 300,
  MAX_NPC_PER_SCENE: 3,
};
